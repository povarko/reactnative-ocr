import { StyleSheet } from "react-native";
import { Colors } from "@AppTheme";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000"
  },
  centerText: {
    flex: 1,
    fontSize: 18,
    padding: 32,
    fontWeight: "600",
    color: "#777"
  }
});
