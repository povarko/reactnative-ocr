import EStyleSheet from "react-native-extended-stylesheet";

export const stylesheetConfig = function() {
  // Add style themes that will be available globally
  EStyleSheet.build({
    //$tintColor: "#521396",
    //$textColor: "#2E3841"
  });
};
